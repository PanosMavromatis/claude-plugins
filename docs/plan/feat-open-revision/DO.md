# feat/open-revision

**Status**: active
**Created**: 2026-08-30
**Subgoal**: Give revisions a full lifecycle, identified by label

## Tasks

- [x] Write `scripts/open-revision.sh <label>`: validate the label, refuse if a revision with it already exists (heading or directory), and print the section that would be inserted plus where. Read-only.
  > **Done:** `scripts/open-revision.sh <label>` validates the label (non-empty, no spaces or slashes, no leading `#`/`-`), refuses when a section, a directory, or a Closed-revisions entry already exists for it, and prints the section plus the insertion point above `## Closed revisions` / `## Deferred`. Read-only.
- [x] Write `commands/open-revision.md` following the `/close-revision` precedent — script in `allowed-tools`, writes behind confirmation, section inserted above `## Closed revisions` / `## Deferred`.
  > **Done:** `commands/open-revision.md` follows the `/close-revision` precedent. Two rules it states explicitly: never invent a label, and never leave the placeholder preamble — a revision's preamble is the one part of the lifecycle no command can generate, and a placeholder is worse than nothing because it looks like a preamble. It also does not create the directory: an empty directory is untracked by git, so it would be lost or need a pointless placeholder file.
- [x] Switch `scripts/close-revision.sh` from number to label: match `## Subgoals — revision <label>`, derive the destination `docs/plan/<label>/_DO.md`. Keep refusing on open items, missing section, existing destination.
  > **Done:** Both scripts match on `## Subgoals — revision <label>` and derive `docs/plan/<label>/`. Caught a platform bug in the process: the first version matched with a BRE alternation `\(…\|…\)`, which BSD `grep` does not support — it matches nothing rather than erroring, so `/close-revision` silently failed to find any section on macOS. Replaced with an exact `awk` comparison, which also sidesteps regex metacharacters in a label and correctly refuses a prefix match (`07-thing` must not match `07-thing-extended`). Also fixed BSD `wc -l` padding, which produced `lines 3-       5` in the report.
- [x] Switch `scripts/file-plans.sh` to derive `docs/plan/<label>/` from the heading, and fix the `git mv` destination bug by having `/file-plans` `mkdir -p` before moving. Test that a plan files into a revision directory that does not yet exist.
  > **Done:** `/file-plans` now `mkdir -p`s each destination before moving, and `Bash(mkdir:*)` was added to its `allowed-tools`. Demonstrated the bug first: `git mv docs/plan/feat-thing docs/plan/09-fresh/feat-thing` into a missing directory fails with exit 128, and the fixed sequence succeeds. This was latent for two revisions only because `rev-2/` and `rev-3/` had been created by hand during the regrouping cycle, before the command existed — every revision's *first* filing would have hit it.
- [x] Migrate `rev-2/` → `02-mcp-github-access/` and `rev-3/` → `03-subgoal-plan-management/`, updating the master-plan heading and the closed-revisions pointer. Verify resolution afterwards.
  > **Done:** `rev-2/` → `02-mcp-github-access/`, `rev-3/` → `03-subgoal-plan-management/`, both as git renames, with the master-plan heading, the archive's own heading and the closed-revisions pointer updated. Verified after: rung 4 returns 16 matches and still excludes the archive, both renamed revisions' branch plans resolve by name, and the archive remains reachable by explicit path. Revision 1's bare `## Subgoals` heading is left alone — it has no revision identifier and never had one.
- [x] Update README and `CLAUDE.md`: the open→work→file→close lifecycle, labels rather than numbers, and the directory-naming rule.
  > **Done:** README's lifecycle section now opens with the revision, its diagram starts at `/open-revision`, and the command table gained a row. `CLAUDE.md` documents the label-as-identity rule, why `/open-revision` does not create the directory, why `/file-plans` must `mkdir -p`, and the ordering wrinkle that a revision's last branch plan cannot be filed before the revision closes.
