# feat/plan-targeted-access

**Status**: active
**Created**: 2026-08-29
**Subgoal**: Make master-plan access cost independent of file size

## Tasks

- [x] Rewrite `/smart-merge` step 7's master-plan update as locate-then-window: `grep -n '> \*\*Branch:\*\* <branch>'` to find the backlink, read a bounded window around the hit, edit in place. State the sequence explicitly — an LLM told only "update the master plan" will default to a whole-file read, which is the entire cost being avoided.
  > **Done:** Step 7's master-plan update is now an explicit three-move sequence — `Grep` for the backlink with line numbers, `Read` a bounded window (~10 before / 15 after), edit in place — followed by a paragraph stating why the sequence must be followed literally, with the measured numbers. Uses the `Grep` tool rather than shell `grep`: `Grep` is allow-listed in this command and `Bash(grep:*)` is not, so a shell grep would have prompted on every invocation. No frontmatter change needed.
- [x] Add the zero-match guard: if the `grep` returns no hit, report it. "No master-plan item is backlinked to this branch" (legitimate — a standalone branch) must be distinguishable from "the lookup failed" (a bug). Today both produce silence. Also handle >1 hit, which means a duplicated backlink.
  > **Done:** Three outcomes spelled out: one hit (proceed), no hits (report, and distinguish a legitimately standalone branch from a lost or misspelled backlink — plus flag truncation as a possible cause on a large file), more than one hit (duplicated backlink; list line numbers and ask, never update both). The rationale is stated in the command: an unreported miss is indistinguishable from having had nothing to do.
- [x] Apply the same treatment to rung 3 in `/step` and `/hitl-step`: locate the next unchecked item by `grep -n`, read the header plus a window rather than the whole file. Decide whether to keep a whole-file read below a size threshold, and say why in the prose. **Their Step 1 sections are byte-identical by contract** — land it in both and verify by diff.
  > **Done:** Rung 3 gained the read rule in both commands, byte-identical and re-verified by diff. Step 2 differs between the two — plain checkbox versus three-tier marker model — so it got per-file wording: `/step` greps `^- \[ \]`, `/hitl-step` greps `^- \[[ ~]\]`. Both keep whole-file reads for branch plans, where they are cheap and give useful context. Both also gained the completion guard: on a large master plan, confirm with `Grep` before reporting all-complete, since a truncated read makes that a false report rather than a finding.
- [x] Record the rule and the measurement in `CLAUDE.md`: master-plan access is locate-then-window, a silent miss is a bug, and the numbers that justify it. Retire the Deferred item, noting that the split-into-milestone-files design it proposed was rejected *by measurement* — size was never the problem.
  > **Found while doing this:** `/step` allow-listed `Bash(cat:*)` and `Bash(find:*)` while its body used neither — the one apparent `find` is the English word in prose. Removed. `cat` matters beyond tidiness: it permits an unbounded whole-file read of exactly the file this branch is teaching the command not to read whole, so the allow-list was quietly underwriting the behaviour the prose now forbids. Narrowing an allow-list is never destructive, so this is safe to fold in here rather than deferring. `/hitl-step` was already clean.
  > **Found while doing this:** targeting the *read* alone would have left the saving half-done. `/step` lacked `Edit` entirely, so "save the plan file" meant a whole-file `Write` — as expensive as the read just removed — and both commands' Step 5 said to "re-read the plan file" every iteration, multiplying that cost by N in a loop built around iterating. Added `Edit` to `/step`, and both commands now say to edit in place and to re-`Grep` rather than re-read. `Write` is kept in both for creating a plan file from scratch.
  > **Done:** `CLAUDE.md` gained the locate-then-window rule with the measurement, a note that the split-into-milestone-files design was rejected by measurement and should not return without new evidence, the `Grep`-not-`Bash(grep:*)` allow-list trap, and a general **a silent miss is a bug** rule tying this to the path-reconstruction failure from PR #8. The Deferred item was retired in the master plan when the branch was opened.
