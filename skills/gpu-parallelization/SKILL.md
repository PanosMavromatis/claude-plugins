---
name: gpu-parallelization
description: "TRIGGER: when parallelizing an algorithm with Numba CUDA, writing a cuda.jit kernel, or advancing to Phase 4 (the cuda phase). Do NOT trigger for the CPU-parallel phase 3 (@njit(parallel=True) with prange) — that is cpu-parallelization."
---

# gpu-parallelization

Guide the Numba CUDA parallelization of a Phase 2 Cython algorithm (Phase 3).

TODO: implement this skill.
